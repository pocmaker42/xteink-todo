/**
 * Copy this file to secrets.h and fill in your values.
 * secrets.h is NOT committed (.gitignore).
 */
#pragma once

// WiFi
#ifndef WIFI_SSID
#define WIFI_SSID "YOUR_SSID"
#endif

#ifndef WIFI_PASSWORD
#define WIFI_PASSWORD "YOUR_PASSWORD"
#endif

// Full URL to todos.php
#ifndef API_TODOS
#define API_TODOS "https://example.com/xteink/todos.php"
#endif

// Optional token (must match server/config.php → api_token)
#ifndef API_TOKEN
#define API_TOKEN ""
#endif
