<?php
/**
 * Copy this file to config.php and adjust the values.
 * config.php is NOT committed (.gitignore).
 */

return [
    // Public base URL (optional, useful for docs / links)
    'base_url' => 'https://example.com/xteink',

    // Optional API token: if set, device and UI must send it
    // Header: X-Api-Token: ...  or  query ?token=...
    'api_token' => '',
];
